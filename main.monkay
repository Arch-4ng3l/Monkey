var w = newWindow(10, 10, "hello");
var label = addLabel(w, " ");
var edit = addEdit(w, "Type Something");
var click = func() {
    write(label, read(edit));
}

addButton(w, "click me", click);

addCloseButton(w);
openWindow(w);

